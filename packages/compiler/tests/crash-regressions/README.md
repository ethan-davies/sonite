# Crash regression fixtures
#
# Each subdirectory contains `input.sn` — a minimized source that previously
# caused an internal compiler error. `crash-regressions.test.ts` asserts that
# compiling each fixture no longer throws.
